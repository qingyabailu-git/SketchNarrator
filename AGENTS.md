# AGENTS.md

## 范围与入口

本文件管理 SketchNarrator 的开发与维护。唯一 Skill ID 为 `sketch-narrator`，用户入口是 [SKILL.md](SKILL.md)；`renderer/` 是内置组件，不是第二个 Skill。宿主使用方式与安装前提见 [README.md](README.md)。

生产流程由 SKILL.md 定义。保存、撤销、对象身份、掩码、批准和帧预算的唯一契约是 [timing-and-recovery.md](references/timing-and-recovery.md)；视觉表达见 [unified-visual-language.md](references/unified-visual-language.md)。改动这些行为时先读对应契约，同步实现与调用方，不在本文件重复参数和步骤。

## 修改边界

- 只有用户明确要求开发、调试或审查工具时才处理源码。普通视频任务只调用公开入口、修改项目文件，不因媒体结果顺手修工具或运行源码测试。
- 源码、安装入口在生产中只读。使用前已授权的公开安装命令可以写入隔离环境；环境维护不授权修改源码，生产中不隐式升级依赖。
- 先检查现有修改，保留用户工作；修改前建立一个任务级备份，后续原件放入同一目录。不得从归档源覆盖当前实现。
- 用户只要求分析时不修改；明确限制测试时遵从并记录未验证。未获明确要求不提交、推送或发布。
- 修改 Skill 使用可用的 skill-creator；按实际宿主提供的工具工作，不假定其他宿主具有同名工具或相同安装目录。

## 文件职责

| 位置 | 职责 |
| --- | --- |
| `scripts/` | 项目状态、编排、配音、字幕、合成、工作台服务和上层 QA |
| `renderer/scripts/` | 笔迹、上色、手部、板擦、逐帧渲染与运行环境 |
| `renderer/assets/` | 渲染与工作台所需的公开静态文件 |
| `references/` | 文件契约、视觉规范与专题说明 |
| `assets/` | 可公开分发的风格与通用素材 |
| `tests/`、`renderer/tests/` | 对应层的行为验证 |

## 契约变更

- 先明确应保持的行为，再同步源码、文档、示例与受影响测试；不能为了让失败样本通过而随意放宽规则。
- 用户编辑的持久化与生产有效性分开；执行读取已批准的计划，不能隐式重新规划。具体定义以唯一契约为准。
- Python 与依赖采用最低要求，只有可复现的不兼容证据才加入有原因、有解除条件的版本排除。新版本可安装不代表运行已验证；日常启动复用环境，升级通过显式入口执行。

## 验证

- 文档或素材引用变动核对事实、链接、语言一致性和展示，不自动触发代码或媒体回归。
- 代码改动先验证受影响行为与错误边界；公共契约、共享渲染逻辑或依赖变动再扩大到有关调用方。不要编写只重复实现结果的测试。
- 上层测试位于 `tests/`，底层测试位于 `renderer/tests/`；使用项目隔离解释器运行相应 unittest 集合。只有改动风险需要时才另建隔离短片，不能复用或覆盖用户正式项目作为测试材料。
- 不运行用户禁止的测试。交付区分静态核对、代码测试、实际渲染和视觉 QA；未运行项明确注明。
- 代码检查不代表成片通过；媒体验收按 [final-qa.md](references/final-qa.md) 执行。

## 公开文件与私人资产

- 用户项目、私人 IP、密钥、本机路径、虚拟环境和缓存不得进入公开包。可选 presenter 资产包与配置留在 Skill 外；历史兼容私有目录也不得分发。
- 保留根目录和 renderer 中的许可及第三方说明。不要把第三方或私人素材当作根目录许可自动覆盖的资产。
- 正式文件变动同步 `release-manifest.json`；公开包只从清单集合构建，不直接压缩开发目录。
- 完整核对交付文件路径集合，抽查关键文件哈希，记录包整体 SHA256；不默认逐文件全量复核。清单记录全体文件哈希不等于要求每次重新对照全部哈希。
- 文本文件使用 UTF-8 与 LF。交付说明实际改动、备份、验证证据及剩余限制，不把安装成功或文件存在当作验收通过。
